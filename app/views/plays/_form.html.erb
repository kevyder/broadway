<%= simple_form_for @play, html: {multipart: true} do |f| %>
  <%= f.file_field :image %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :director %>
  <%= f.select :category_id, @categories, :prompt => "Select Category" %>
  <%= f.button :submit %>
<% end %>